{
  "name": "alo-adventures-quickdeploy",
  "version": "0.1.0",
  "private": true,
  "engines": { "node": "18.x" },
  "scripts": {
    "start": "node server.js",
    "build": "cd client && npm install && npm run build",
    "postinstall": "npm run build"
  },
  "dependencies": {
    "express": "^4.18.2",
    "socket.io": "^4.7.0",
    "cors": "^2.8.5"
  }
}
